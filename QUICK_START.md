# 🚀 QUICK START — Расслоенная эмуляция

## Прочитай сначала

1. Открой `Denuvo DRM Detection Methods.txt`
2. Читай **с конца** до момента, где начинается техническое обсуждение системы
3. Прочитай `TECHNICAL_BRIEFING.md` для полного контекста

## Суть проекта в 3 пунктах

1. **Проблема**: Традиционные эмуляторы/дебаггеры детектируются через рассинхронизацию таймеров (RDTSC vs GetTickCount)

2. **Решение**: Расслоенная эмуляция с **единым источником времени** (`VirtualClock`) для всех уровней:
   - Слой 1: Эмулятор CPU (Unicorn Engine) + VirtualClock
   - Слой 2: Виртуальное ядро ОС (MiniOS)
   - Слой 3: Заглушки WinAPI (критичные + периферия)
   - Слой 4: Целевой код (PE-файлы, анти-тамперы)

3. **Инновация**: 
   - Дифференциальный анализ — автоматическая локализация проверок защиты
   - Фокус на основной ветке — периферия (DirectX, GUI) = заглушки

## Текущее состояние проекта

### ✅ Реализовано (РАБОТАЕТ!)

#### Статический анализ
- `denuvo-detector.py` — детальный анализ PE-файлов
  - Поиск секций, сигнатур, анализ энтропии
  - Batch-режим сканирования
  - Экспорт в JSON/CSV
  
- `denuvo-advanced.py` — система оценки с эвристиками
  - Множественные проверки (секции, сигнатуры, импорты, энтропия)
  - Scoring system (0-100 баллов)

#### Расслоенная эмуляция
- ✅ **VirtualClock** — единый источник времени
  - RDTSC, GetTickCount64, QueryPerformanceCounter синхронизированы
  - Тест: `python demos/test_rdtsc.py`

- ✅ **MiniOS** — виртуальное ядро ОС
  - VirtualMemoryManager (VirtualAlloc/Free)
  - HeapManager (malloc/free)
  - ThreadScheduler (планировщик потоков)
  - Syscall table (12+ системных вызовов Nt*/Zw*)
  - Тест: `python demos/test_mini_os.py`

- ✅ **WinAPIStubs** — заглушки WinAPI
  - Критичные: GetTickCount64, HeapAlloc, VirtualAlloc
  - Периферия: LoadLibrary, CreateFile, MessageBox, Sleep
  - Graceful error handling для неизвестных функций

- ✅ **DifferentialAnalyzer** — дифференциальный анализ
  - Два синхронных эмулятора
  - Автоматическая локализация точки расхождения
  - Дизассемблирование контекста
  - Тест: `python demos/test_differential.py`

- ✅ **Принцип "фокус на основной ветке"**
  - Демонстрация: `python demos/test_main_path_focus.py`

### 🚧 В разработке

1. Углубление дифференциального анализатора (бинарный поиск, визуализация)
2. Тестирование на реальных PE-файлах с защитой
3. Модель кэша (опционально)

3. **Модель кэша** (Этап 2)
4. **Тепловой шум** (Этап 3)
5. **Полноценная виртуальная ОС** (Этап 5)

## Философия разработки

### Принципы

1. **Меньше документации, больше кода** — итеративный подход
2. **Проверяй и исправляй** — тестируй каждый компонент
3. **Только свой код** — анализируем учебные примеры, не коммерческие игры
4. **Семантическая эквивалентность** — каждый слой должен быть неотличим от реального

### Этические границы

- ✓ Создавать учебные анти-тамперы
- ✓ Анализировать собственный код
- ✓ Публиковать результаты как научные материалы
- ✗ Дизассемблировать коммерческие игры
- ✗ Обходить чужую защиту
- ✗ Создавать инструменты для пиратства

## Ключевые концепции

### 1. Композиционная семантическая эквивалентность

```
Если каждый слой N семантически эквивалентен реальному,
И слой N+1 видит только интерфейс слоя N,
То слой N+1 не может детектировать эмуляцию
```

### 2. Единый источник времени

```python
class VirtualClock:
    def __init__(self):
        self.ticks = 0  # Единый счётчик для ВСЕЙ системы
    
    def rdtsc(self):
        return self.ticks
    
    def get_tick_count(self):
        return self.ticks // 3_000_000  # 3 GHz → ms
    
    def query_performance_counter(self):
        return self.ticks * 10
```

### 3. Дифференциальный анализ

```python
# Два идентичных эмулятора
emu_valid = LayeredEmulator(seed=0xCAFE)
emu_invalid = LayeredEmulator(seed=0xCAFE)  # То же семя!

# Разные входные параметры
emu_valid.set_arg(0, "VALID-KEY-1234")
emu_invalid.set_arg(0, "INVALID-KEY-XXX")

# Синхронное выполнение → локализация расхождения
for step in range(MAX_STEPS):
    emu_valid.step()
    emu_invalid.step()
    if emu_valid.state != emu_invalid.state:
        print(f"Расхождение на шаге {step}!")
        break
```

## Следующие действия

### Для начала работы

1. Проверь зависимости:
```bash
pip install pefile unicorn-engine capstone
```

2. Протестируй текущие детекторы:
```bash
python denuvo-detector.py Outlaws.exe
python denuvo-advanced.py Outlaws.exe
```

3. Изучи архитектуру в `TECHNICAL_BRIEFING.md`

### Для продолжения разработки

Начни с **Этапа 1** — минимальной расслоенной эмуляции:

1. Создай `virtual_clock.py` — единый источник времени
2. Создай `layered_emulator.py` — обёртка над Unicorn Engine
3. Создай `time_check_demo.c` — учебный анти-тампер
4. Протестируй: анти-тампер не должен детектировать эмуляцию

## Структура проекта

```
.
├── denuvo-detector.py          # Детальный анализ PE
├── denuvo-advanced.py          # Система оценки
├── detect.py                   # Простой детектор (legacy)
├── detect-fast.py              # Быстрый поиск (legacy)
├── RoadMap.md                  # План развития
├── TECHNICAL_BRIEFING.md       # Полное техническое описание
├── QUICK_START.md              # Этот файл
└── Denuvo DRM Detection Methods.txt  # Полная переписка с Qwen

Планируется:
├── src/
│   ├── core/
│   │   ├── virtual_clock.py    # Единый источник времени
│   │   ├── layered_emulator.py # Расслоенная эмуляция
│   │   └── cache_model.py      # Модель кэш-иерархии
│   ├── analysis/
│   │   ├── differential.py     # Дифференциальный анализатор
│   │   └── visualizer.py       # Визуализация результатов
│   └── demos/
│       ├── time_check_demo.c   # Учебный анти-тампер
│       └── vm_demo.c           # Виртуализация кода
└── tests/
    └── test_semantic_equiv.py  # Тесты семантической эквивалентности
```

## Полезные команды

```bash
# Детальный анализ одного файла
python denuvo-detector.py game.exe

# Batch-сканирование директории
python denuvo-detector.py -d "C:/Games" -r

# Экспорт результатов
python denuvo-detector.py -d . -o results.json
python denuvo-detector.py -d . -o results.csv -f csv

# Тихий режим (только результат)
python denuvo-detector.py -q game.exe

# Продвинутый анализ с оценкой
python denuvo-advanced.py game.exe
```

## Контакты и ресурсы

- **Проект**: Расслоенная эмуляция для анализа анти-тампер систем
- **Дата начала**: Январь 2026
- **Статус**: Proof of Concept → Минимальная реализация

---

**Помни**: Это научный проект. Анализируем только свой код. Цель — понимание, а не обход чужой защиты.
