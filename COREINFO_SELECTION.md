# CoreInfo - Выбор консольной утилиты для эмуляции

## Дата: 2026-02-01

## 🎯 ПРОБЛЕМА С CPU-Z

CPU-Z требует GUI-окружение и выходит с кодом 255 при отсутствии:
- Проверяет MessageBoxA, GetActiveWindow, GetLastActivePopup
- Проверяет Window Station через GetProcessWindowStation
- Вызывает ExitProcess(255) до проверки командной строки
- Параметр `-txt=report` не используется

## 🔍 ПОИСК АЛЬТЕРНАТИВ

### Протестированные утилиты:

1. **HWiNFO** ❌
   - Консольный режим только в Pro версии
   - Бесплатная версия показывает: "Command-line parameters are supported in HWiNFO64 Pro only"

2. **SIV (System Information Viewer)** ❌
   - Требует GUI
   - Зависает при запуске с параметром `-h`

3. **WMIC** ✅ (встроенная)
   - Работает: `wmic cpu get name,numberofcores,maxclockspeed`
   - Но слишком простая, не подходит для тестирования эмуляции

4. **CoreInfo (Sysinternals)** ✅✅✅ **ПОБЕДИТЕЛЬ!**

## 🏆 COREINFO - ИДЕАЛЬНЫЙ КАНДИДАТ

### Преимущества:

✅ **Чисто консольная утилита**
- Нет GUI-проверок
- Работает из командной строки
- Выводит результат в stdout

✅ **От Microsoft**
- Часть Sysinternals Suite
- Хорошо документирована
- Активно поддерживается

✅ **Использует драйвер**
- Устанавливает COREINFO100.SYS
- Низкоуровневый доступ к CPU
- Отличный тест для эмуляции драйверов

✅ **Детальная информация**
- CPU topology
- Cache hierarchy
- NUMA nodes
- CPU features (SSE, AVX, etc.)

### Технические детали:

```
Файл: Coreinfo64.exe
Размер: ~200KB
Архитектура: x64
Импортов: 151 (vs 634 у CPU-Z)
```

### Первый запуск в эмуляторе:

```
[*] Loading: Coreinfo64.exe
[*] Arguments: -accepteula
[MEM] Allocated 1024MB at 0x7fff0000 for stubs
[+] Loaded 436 generated stubs
[+] Registered 28 custom implementations
[*] Imports: 151, patched: 151
[*] Entry point: 0x14000fb00
[*] Инструкций: 14
[!] Unknown stub at 0x7fffbb01
```

**Результат:** CoreInfo загрузился и выполнил 14 инструкций!

## 📊 СРАВНЕНИЕ

| Характеристика | CPU-Z | CoreInfo |
|----------------|-------|----------|
| GUI | Требуется | Нет |
| Консольный режим | Только с `-txt=report` | Да |
| Импортов | 634 | 151 |
| Размер | 7.2MB | ~200KB |
| Драйвер | cpuz_x64.sys | COREINFO100.SYS |
| Инструкций до ошибки | 1,575 (ExitProcess) | 14 (Unknown stub) |
| Проверка GUI | Да (выходит) | Нет |
| От Microsoft | Нет | Да (Sysinternals) |

## 🎯 СЛЕДУЮЩИЕ ШАГИ

### Краткосрочно (для CoreInfo):
1. ✅ Загрузить CoreInfo в эмулятор
2. 🔄 Найти неизвестную заглушку @ 0x7fffbb01
3. 🔄 Реализовать недостающие функции
4. 🔄 Добавить поддержку драйвера COREINFO100.SYS

### Среднесрочно:
1. Реализовать DeviceIoControl для драйвера
2. Эмулировать CPUID инструкцию
3. Добавить MSR (Model-Specific Registers) эмуляцию
4. Реализовать NUMA topology

### Долгосрочно:
1. Полная эмуляция CoreInfo
2. Возврат к CPU-Z с GUI-эмуляцией
3. Поддержка других Sysinternals утилит
4. Production-ready эмулятор

## 💡 ВЫВОДЫ

**CoreInfo - лучший выбор для продолжения работы!**

✅ **Преимущества:**
- Нет GUI-проверок (главное!)
- Меньше импортов (151 vs 634)
- Меньше размер (~200KB vs 7.2MB)
- От Microsoft (хорошая документация)
- Использует драйвер (интересный тест)

✅ **Прогресс:**
- Загружается успешно
- Выполняет 14 инструкций
- Останавливается на неизвестной заглушке (легко исправить)

✅ **Перспективы:**
- Быстрее довести до работы
- Проще отлаживать
- Меньше зависимостей
- Лучше для тестирования эмуляции

---

**Статус:** 🟢 CoreInfo выбран как основная цель для эмуляции

**Следующий шаг:** Найти и реализовать неизвестную заглушку @ 0x7fffbb01

**Коммит:** Selected CoreInfo as primary emulation target (console-only, 151 imports, Microsoft Sysinternals)
